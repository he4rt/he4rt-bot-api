---
type: plan
title: 'Live fase 2 — chat via Reverb, admin e robustez'
module: live
status: proposed
date: 2026-08-29
author: Clintonrocha98
related:
    spec: 2026-08-29-live-fase-2-chat-admin-robustez
---

# Plano de Implementação — Live fase 2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Live como entidade de domínio (key por live, ciclo de vida automático), chat em tempo real via Reverb persistido no pipeline de mensagens do activity, gestão no panel-admin e follow-ups de robustez do MVP.

**Architecture:** O módulo `live` ganha model/migrations e passa a validar o ingest contra o banco; o mediamtx (variante `-ffmpeg`) notifica sinal via webhook; o chat grava em `messages` (provider `He4rtLives`, identidade sintética por usuário) e broadcasta em canal público `live.{uuid}` via Reverb; o portal consome via Echo; o panel-admin gerencia via Filament.

**Tech Stack:** Laravel 12/PHP 8.5, laravel/reverb, laravel-echo + pusher-js, Livewire 3, Filament 5, Pest 5, hls.js, mediamtx v1.20.1-ffmpeg, Redis (phpredis).

**Spec:** `docs/specs/2026-08-29-live-fase-2-chat-admin-robustez.md` (autoridade em conflitos). MVP: `docs/specs/2026-08-29-live-mvp-design.md`.

## Global Constraints

- **Comentários em código são proibidos.** Única exceção: docstring de UMA linha em função/classe/tipo.
- Todo PHP: `declare(strict_types=1);`, classes `final`, tipos explícitos em parâmetros e retorno.
- DTOs `final readonly class`; nunca array associativo cruzando fronteira.
- Actions (ação única), nunca `*Service`; sem Repository; interfaces = `*Contract` em `Contracts/`.
- Dependência intra-repo em composer.json SEMPRE `"he4rt/x": "^1.0.0"`.
- **Migrations:** SEMPRE via `php artisan make:migration ... --module=live --no-interaction`; colunas de data SEMPRE `Tz` (`timestampTz`/`timestampsTz`); PHPDoc `@property` do model atualizado no MESMO commit; models com `#[Table]` + `#[UseFactory]` explícitos.
- **Enums novos** implementam `HasLabel`+`HasColor`+`HasDescription` (Filament), `match` sem `default`.
- **Casts JSON soltos são banidos** (`NoLooseArrayCastsTest`) — nenhum model novo com cast `array`.
- Strings de UI em pt_BR.
- Commits: conventional commits `<type>(<escopo>): ...` em pt_BR. **NUNCA** `Co-Authored-By` nem citar Claude.
- Após editar PHP: `vendor/bin/pint --dirty --format agent`.
- Testes: SOMENTE o subconjunto do que mudou (ex.: `./vendor/bin/pest app-modules/live/tests`) — **sem `--parallel`**.
- PHPStan por tarefa: `vendor/bin/phpstan analyse app-modules/<modulo>/src --ansi`.
- lint-staged roda prettier em js/yml/md no commit (reformatações esperadas); NÃO editar husky; se commit falhar pelo hook, reportar (não usar `--no-verify`).
- Implementadores NÃO despacham subagentes.
- Skills: implementadores de PHP/Laravel ativam `laravel-best-practices`; testes → `pest-testing`; módulo → `modular`; Livewire/portal → `livewire-specialist` e `tailwindcss-development`; Filament → pesquisar `search-docs` antes.

---

### Task 1: `IdentityProvider::He4rtLives` (identity)

**Files:**

- Modify: `app-modules/identity/src/ExternalIdentity/Enums/IdentityProvider.php`
- Test: `app-modules/identity/tests/Feature/He4rtLivesProviderTest.php`

**Interfaces:**

- Produces: `IdentityProvider::He4rtLives` (`'he4rt-lives'`), utilizável em `ResolveUserProviderDTO`/`ResolveExternalIdentity` sem mudanças nessas classes.

- [ ] **Step 1: Teste que falha**

```php
<?php

declare(strict_types=1);

use He4rt\Identity\ExternalIdentity\Enums\IdentityProvider;

it('expõe o provider da plataforma de lives da he4rt', function (): void {
    $provider = IdentityProvider::from('he4rt-lives');

    expect($provider)->toBe(IdentityProvider::He4rtLives)
        ->and($provider->getLabel())->not->toBeEmpty()
        ->and($provider->getColor())->not->toBeNull();
});
```

Run: `./vendor/bin/pest app-modules/identity/tests/Feature/He4rtLivesProviderTest.php`
Expected: FAIL (`ValueError: not a valid backing value`).

- [ ] **Step 2: Adicionar o caso e preencher TODOS os `match ($this)`**

Adicionar `case He4rtLives = 'he4rt-lives';` em ordem alfabética entre os cases. Em seguida, abrir o enum inteiro e preencher o caso novo em **cada** método com `match ($this)` (ex.: `getLabel` → `'He4rt Lives'`, `getColor`, `getIcon`, `getDescription`, `getType` → `IdentityType::External`, e quaisquer outros — OAuth config, URLs etc. — com o valor neutro que o método pedir; se um método não fizer sentido p/ provider interno, retornar o mesmo fallback que providers não-OAuth como `Domain` usam). O PHPStan acusa `match` não exaustivo — usar isso como checklist.

- [ ] **Step 3: Rodar teste do enum + suíte do identity**

Run: `./vendor/bin/pest app-modules/identity/tests/Feature/He4rtLivesProviderTest.php`
Expected: PASS.
Run: `./vendor/bin/pest app-modules/identity/tests`
Expected: PASS (nenhuma regressão — testes que iteram `IdentityProvider::cases()` podem precisar de ajuste consciente; reportar se algum fixar lista de providers).

- [ ] **Step 4: Checks e commit**

Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/identity/src --ansi`

```bash
git add app-modules/identity
git commit -m 'feat(identity): provider He4rtLives para atividade nativa da plataforma'
```

---

### Task 2: Domínio `Live` — model, enum, migrations e ciclo de vida

**Files:**

- Create (artisan): migration `create_lives_table` em `app-modules/live/database/migrations/`
- Create: `app-modules/live/src/Models/Live.php`
- Create: `app-modules/live/src/Enums/LiveStatus.php`
- Create: `app-modules/live/database/factories/LiveFactory.php`
- Create: `app-modules/live/src/Actions/CreateLive.php`
- Create: `app-modules/live/src/Actions/EndLive.php`
- Create: `app-modules/live/src/Actions/RotateStreamKey.php`
- Create: `app-modules/live/src/Actions/MarkLiveOnline.php`
- Create: `app-modules/live/src/Events/LiveStarted.php`
- Create: `app-modules/live/src/Events/LiveEnded.php`
- Create: `app-modules/live/src/Exceptions/CurrentLiveAlreadyExists.php`
- Modify: `app-modules/live/composer.json` (autoload factories) e `src/LiveServiceProvider.php` (loadMigrationsFrom)
- Test: `app-modules/live/tests/Feature/LiveLifecycleTest.php`

**Interfaces:**

- Produces:
    - `He4rt\Live\Models\Live` (`@property string $id, string $title, ?string $description, LiveStatus $status, string $stream_key, ?CarbonInterface $started_at, ?CarbonInterface $ended_at, int $peak_viewers`), scope `current()` (status ≠ Ended).
    - `He4rt\Live\Enums\LiveStatus: string` — `Created='created'`, `OnAir='on_air'`, `Ended='ended'`.
    - `CreateLive::execute(string $title, ?string $description): Live` (gera `stream_key` = `Str::random(40)`; lança `CurrentLiveAlreadyExists` se houver corrente).
    - `EndLive::execute(Live $live): Live` (Ended + `ended_at`, dispara `LiveEnded`).
    - `RotateStreamKey::execute(Live $live): Live` (nova key).
    - `MarkLiveOnline::execute(Live $live): Live` (OnAir, `started_at ??= now()`, dispara `LiveStarted` em toda chamada).
    - Eventos `He4rt\Live\Events\LiveStarted|LiveEnded` (`ShouldBroadcastNow`, canal `new Channel('live.'.$liveId)`, `broadcastAs(): 'LiveStarted'|'LiveEnded'`, payload `['liveId' => ...]`).

- [ ] **Step 1: Migration**

Run: `php artisan make:migration create_lives_table --module=live --no-interaction`

```php
Schema::create('lives', static function (Blueprint $table): void {
    $table->uuid('id')->primary();
    $table->string('title');
    $table->text('description')->nullable();
    $table->string('status')->default('created');
    $table->text('stream_key');
    $table->timestampTz('started_at')->nullable();
    $table->timestampTz('ended_at')->nullable();
    $table->integer('peak_viewers')->default(0);
    $table->timestampsTz();
});

DB::statement("CREATE UNIQUE INDEX lives_single_current_unique ON lives ((1)) WHERE status <> 'ended'");
```

No `down()`: `Schema::dropIfExists('lives')` (o índice cai junto).

- [ ] **Step 2: Teste que falha (ciclo de vida)**

`app-modules/live/tests/Feature/LiveLifecycleTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Live\Actions\CreateLive;
use He4rt\Live\Actions\EndLive;
use He4rt\Live\Actions\MarkLiveOnline;
use He4rt\Live\Actions\RotateStreamKey;
use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Events\LiveEnded;
use He4rt\Live\Events\LiveStarted;
use He4rt\Live\Exceptions\CurrentLiveAlreadyExists;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\Event;

it('cria uma live com stream key gerada', function (): void {
    $live = resolve(CreateLive::class)->execute('Retrô He4rt', 'Balanço do mês');

    expect($live->status)->toBe(LiveStatus::Created)
        ->and($live->stream_key)->toHaveLength(40)
        ->and($live->peak_viewers)->toBe(0)
        ->and(Live::query()->current()->sole()->id)->toBe($live->id);
});

it('impede duas lives correntes ao mesmo tempo', function (): void {
    resolve(CreateLive::class)->execute('Primeira', null);

    resolve(CreateLive::class)->execute('Segunda', null);
})->throws(CurrentLiveAlreadyExists::class);

it('permite criar nova live depois de encerrar a anterior', function (): void {
    $first = resolve(CreateLive::class)->execute('Primeira', null);
    resolve(EndLive::class)->execute($first);

    $second = resolve(CreateLive::class)->execute('Segunda', null);

    expect(Live::query()->current()->sole()->id)->toBe($second->id);
});

it('marca online com started_at apenas na primeira vez e re-emite o evento', function (): void {
    Event::fake([LiveStarted::class]);
    $live = resolve(CreateLive::class)->execute('Retrô', null);

    $live = resolve(MarkLiveOnline::class)->execute($live);
    $firstStart = $live->started_at;

    $this->travel(5)->minutes();
    $live = resolve(MarkLiveOnline::class)->execute($live);

    expect($live->status)->toBe(LiveStatus::OnAir)
        ->and($live->started_at?->equalTo($firstStart))->toBeTrue();
    Event::assertDispatchedTimes(LiveStarted::class, 2);
});

it('encerra a live e broadcasta o fim', function (): void {
    Event::fake([LiveEnded::class]);
    $live = resolve(CreateLive::class)->execute('Retrô', null);

    $live = resolve(EndLive::class)->execute($live);

    expect($live->status)->toBe(LiveStatus::Ended)
        ->and($live->ended_at)->not->toBeNull();
    Event::assertDispatched(LiveEnded::class, fn (LiveEnded $event): bool => $event->liveId === $live->id);
});

it('rotaciona a stream key', function (): void {
    $live = resolve(CreateLive::class)->execute('Retrô', null);
    $original = $live->stream_key;

    $live = resolve(RotateStreamKey::class)->execute($live);

    expect($live->stream_key)->toHaveLength(40)->not->toBe($original);
});
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/LiveLifecycleTest.php`
Expected: FAIL (classes inexistentes).

- [ ] **Step 3: Enum, model, factory**

`src/Enums/LiveStatus.php` — contratos Filament, cores não-ordenadas:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Enums;

use Filament\Support\Contracts\HasColor;
use Filament\Support\Contracts\HasDescription;
use Filament\Support\Contracts\HasLabel;

/** Estágio editorial de uma live: criada no admin, no ar com sinal, encerrada. */
enum LiveStatus: string implements HasColor, HasDescription, HasLabel
{
    case Created = 'created';
    case OnAir = 'on_air';
    case Ended = 'ended';

    public function getLabel(): string
    {
        return match ($this) {
            self::Created => 'Criada',
            self::OnAir => 'No ar',
            self::Ended => 'Encerrada',
        };
    }

    public function getColor(): string
    {
        return match ($this) {
            self::Created => 'gray',
            self::OnAir => 'success',
            self::Ended => 'danger',
        };
    }

    public function getDescription(): ?string
    {
        return match ($this) {
            self::Created => 'Aguardando o primeiro sinal do OBS',
            self::OnAir => 'Transmissão em andamento',
            self::Ended => 'Encerrada pelo admin — a stream key não vale mais',
        };
    }
}
```

`src/Models/Live.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Models;

use Carbon\CarbonInterface;
use He4rt\Live\Database\Factories\LiveFactory;
use He4rt\Live\Enums\LiveStatus;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Concerns\HasUuids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

/**
 * @property string $id
 * @property string $title
 * @property string|null $description
 * @property LiveStatus $status
 * @property string $stream_key
 * @property CarbonInterface|null $started_at
 * @property CarbonInterface|null $ended_at
 * @property int $peak_viewers
 * @property CarbonInterface|null $created_at
 * @property CarbonInterface|null $updated_at
 */
#[UseFactory(factoryClass: LiveFactory::class)]
#[Table(name: 'lives')]
final class Live extends Model
{
    /** @use HasFactory<LiveFactory> */
    use HasFactory;
    use HasUuids;

    /** @param Builder<$this> $query */
    protected function scopeCurrent(Builder $query): void
    {
        $query->where('status', '!=', LiveStatus::Ended);
    }

    /** @return array<string, mixed> */
    protected function casts(): array
    {
        return [
            'status' => LiveStatus::class,
            'stream_key' => 'encrypted',
            'started_at' => 'datetime',
            'ended_at' => 'datetime',
        ];
    }
}
```

`database/factories/LiveFactory.php` (namespace `He4rt\Live\Database\Factories`; conferir autoload no composer.json do módulo e adicionar `"He4rt\\Live\\Database\\Factories\\": "database/factories/"` se faltar):

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Database\Factories;

use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Models\Live;
use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Str;

/** @extends Factory<Live> */
final class LiveFactory extends Factory
{
    protected $model = Live::class;

    public function definition(): array
    {
        return [
            'title' => $this->faker->sentence(3),
            'description' => $this->faker->optional()->paragraph(),
            'status' => LiveStatus::Created,
            'stream_key' => Str::random(40),
            'peak_viewers' => 0,
        ];
    }

    public function onAir(): self
    {
        return $this->state(['status' => LiveStatus::OnAir, 'started_at' => now()]);
    }

    public function ended(): self
    {
        return $this->state(['status' => LiveStatus::Ended, 'started_at' => now()->subHour(), 'ended_at' => now()]);
    }
}
```

No `LiveServiceProvider`, adicionar `boot()` com `$this->loadMigrationsFrom(__DIR__.'/../database/migrations');`.

- [ ] **Step 4: Actions, eventos e exceção**

`src/Exceptions/CurrentLiveAlreadyExists.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Exceptions;

use DomainException;

final class CurrentLiveAlreadyExists extends DomainException
{
    public static function make(): self
    {
        return new self('Já existe uma live corrente. Encerre-a antes de criar outra.');
    }
}
```

`src/Actions/CreateLive.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Actions;

use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Exceptions\CurrentLiveAlreadyExists;
use He4rt\Live\Models\Live;
use Illuminate\Support\Str;

/** Cria uma live com stream key própria, garantindo uma única live corrente. */
final readonly class CreateLive
{
    public function execute(string $title, ?string $description): Live
    {
        if (Live::query()->current()->exists()) {
            throw CurrentLiveAlreadyExists::make();
        }

        return Live::query()->create([
            'title' => $title,
            'description' => $description,
            'status' => LiveStatus::Created,
            'stream_key' => Str::random(40),
        ]);
    }
}
```

`src/Actions/EndLive.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Actions;

use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Events\LiveEnded;
use He4rt\Live\Models\Live;

/** Encerra a live: a stream key deixa de valer e o front é avisado. */
final readonly class EndLive
{
    public function execute(Live $live): Live
    {
        $live->update(['status' => LiveStatus::Ended, 'ended_at' => now()]);

        LiveEnded::dispatch($live->id);

        return $live->refresh();
    }
}
```

`src/Actions/RotateStreamKey.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Actions;

use He4rt\Live\Models\Live;
use Illuminate\Support\Str;

/** Gera uma nova stream key: a anterior deixa de valer na próxima conexão. */
final readonly class RotateStreamKey
{
    public function execute(Live $live): Live
    {
        $live->update(['stream_key' => Str::random(40)]);

        return $live->refresh();
    }
}
```

`src/Actions/MarkLiveOnline.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Actions;

use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Events\LiveStarted;
use He4rt\Live\Models\Live;

/** Reage ao sinal online do mediamtx: primeira vez marca início; reconexões só re-avisam o front. */
final readonly class MarkLiveOnline
{
    public function execute(Live $live): Live
    {
        $live->update([
            'status' => LiveStatus::OnAir,
            'started_at' => $live->started_at ?? now(),
        ]);

        LiveStarted::dispatch($live->id);

        return $live->refresh();
    }
}
```

`src/Events/LiveStarted.php` (LiveEnded é idêntico trocando nome/broadcastAs):

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Events;

use Illuminate\Broadcasting\Channel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcastNow;
use Illuminate\Foundation\Events\Dispatchable;

/** Sinal da live disponível — emitido a cada runOnOnline do mediamtx. */
final class LiveStarted implements ShouldBroadcastNow
{
    use Dispatchable;

    public function __construct(public string $liveId) {}

    public function broadcastOn(): Channel
    {
        return new Channel('live.'.$this->liveId);
    }

    public function broadcastAs(): string
    {
        return 'LiveStarted';
    }
}
```

- [ ] **Step 5: Rodar testes, checks e commit**

Run: `./vendor/bin/pest app-modules/live/tests/Feature/LiveLifecycleTest.php`
Expected: PASS (6 testes).
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/live/src --ansi`

```bash
git add app-modules/live
git commit -m 'feat(live): model Live com ciclo de vida e stream key por live'
```

---

### Task 3: Ingest fase 2 — auth hook contra o banco, webhook de sinal, StreamStatus com cache

**Files:**

- Modify: `app-modules/live/src/Actions/AuthorizeMediaServerAction.php`
- Create: `app-modules/live/src/IngestWebhookController.php`
- Modify: `app-modules/live/routes/live-routes.php`
- Modify: `app-modules/live/config/live.php`
- Rename+Modify: `app-modules/live/src/DTOs/LiveStatus.php` → `app-modules/live/src/DTOs/StreamStatus.php`
- Modify: `app-modules/live/src/Actions/CheckLiveStatusAction.php` (retorno `StreamStatus` + cache 5s)
- Modify: `bootstrap/app.php` (exceção CSRF p/ `live/ingest/webhook`, espelhando a do auth)
- Test: Modify `app-modules/live/tests/Feature/IngestAuthTest.php`; Create `app-modules/live/tests/Feature/IngestWebhookTest.php`; Modify `app-modules/live/tests/Feature/CheckLiveStatusActionTest.php`

**Interfaces:**

- Consumes (Task 2): `Live::query()->current()`, `MarkLiveOnline::execute(Live): Live`, `LiveStatus` enum.
- Produces: `POST /live/ingest/webhook` (header `X-Live-Webhook-Secret`; body JSON `{"event": "online"|"offline", "path": "live"}` → 204; secret errado → 403; sem live corrente → 204 no-op); `He4rt\Live\DTOs\StreamStatus` (mesmo shape do antigo `LiveStatus`: `bool $onAir`, `?CarbonImmutable $startedAt`, `::offline()`); config novas `live.webhook_secret` (`LIVE_WEBHOOK_SECRET`) — e config `live.stream_key` REMOVIDA.

- [ ] **Step 1: Atualizar testes do auth hook (falham primeiro)**

Reescrever `IngestAuthTest.php` — key agora vem da live corrente:

```php
<?php

declare(strict_types=1);

use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\RateLimiter;

use function Pest\Laravel\postJson;

function ingestPayload(array $overrides = []): array
{
    return array_merge([
        'user' => 'he4rt',
        'password' => '',
        'ip' => '172.18.0.1',
        'action' => 'publish',
        'path' => 'live',
        'protocol' => 'rtmp',
        'query' => '',
    ], $overrides);
}

it('autoriza leitura sem credenciais', function (): void {
    postJson('/live/ingest/auth', ingestPayload(['action' => 'read', 'protocol' => 'hls']))
        ->assertNoContent();
});

it('autoriza publish com a key da live corrente', function (): void {
    $live = Live::factory()->create();

    postJson('/live/ingest/auth', ingestPayload(['password' => $live->stream_key]))
        ->assertNoContent();
});

it('recusa publish com key incorreta', function (): void {
    Live::factory()->create();

    postJson('/live/ingest/auth', ingestPayload(['password' => 'chave-errada']))
        ->assertForbidden();
});

it('recusa publish quando não existe live corrente', function (): void {
    Live::factory()->ended()->create();

    postJson('/live/ingest/auth', ingestPayload(['password' => 'qualquer']))
        ->assertForbidden();
});

it('recusa publish em path desconhecido mesmo com key correta', function (): void {
    $live = Live::factory()->create();

    postJson('/live/ingest/auth', ingestPayload(['password' => $live->stream_key, 'path' => 'outro']))
        ->assertForbidden();
});

it('recusa ações desconhecidas', function (): void {
    postJson('/live/ingest/auth', ingestPayload(['action' => 'sabotage']))
        ->assertForbidden();
});

it('aplica rate limit por IP após falhas repetidas de publish', function (): void {
    RateLimiter::clear('live-publish:172.18.0.9');
    $live = Live::factory()->create();

    foreach (range(1, 5) as $i) {
        postJson('/live/ingest/auth', ingestPayload(['password' => 'errada', 'ip' => '172.18.0.9']))
            ->assertForbidden();
    }

    postJson('/live/ingest/auth', ingestPayload(['password' => $live->stream_key, 'ip' => '172.18.0.9']))
        ->assertForbidden();
});
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/IngestAuthTest.php`
Expected: FAIL.

- [ ] **Step 2: Reimplementar `AuthorizeMediaServerAction`**

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Actions;

use He4rt\Live\DTOs\IngestAuthPayload;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\RateLimiter;

/** Decide se o mediamtx pode executar a ação: leitura é pública, publish exige a key da live corrente. */
final readonly class AuthorizeMediaServerAction
{
    private const int MAX_FAILURES_PER_MINUTE = 5;

    public function execute(IngestAuthPayload $payload): bool
    {
        return match ($payload->action) {
            'read', 'playback' => true,
            'publish' => $this->authorizePublish($payload),
            default => false,
        };
    }

    private function authorizePublish(IngestAuthPayload $payload): bool
    {
        $rateLimitKey = 'live-publish:'.$payload->ip;

        if (RateLimiter::tooManyAttempts($rateLimitKey, self::MAX_FAILURES_PER_MINUTE)) {
            return false;
        }

        if ($this->publishAllowed($payload)) {
            RateLimiter::clear($rateLimitKey);

            return true;
        }

        RateLimiter::hit($rateLimitKey, 60);

        return false;
    }

    private function publishAllowed(IngestAuthPayload $payload): bool
    {
        if ($payload->path !== config()->string('live.path')) {
            return false;
        }

        $live = Live::query()->current()->first();

        if ($live === null || $payload->password === '') {
            return false;
        }

        return hash_equals($live->stream_key, $payload->password);
    }
}
```

Remover `'stream_key' => env('LIVE_STREAM_KEY', '')` do `config/live.php` e adicionar `'webhook_secret' => env('LIVE_WEBHOOK_SECRET', '')`.

Run: `./vendor/bin/pest app-modules/live/tests/Feature/IngestAuthTest.php`
Expected: PASS.

- [ ] **Step 3: Teste que falha — webhook**

`app-modules/live/tests/Feature/IngestWebhookTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Events\LiveStarted;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\Event;

use function Pest\Laravel\postJson;

beforeEach(function (): void {
    config()->set('live.webhook_secret', 'segredo-do-teste');
});

function webhook(array $body, ?string $secret = 'segredo-do-teste'): \Illuminate\Testing\TestResponse
{
    $headers = $secret === null ? [] : ['X-Live-Webhook-Secret' => $secret];

    return postJson('/live/ingest/webhook', $body, $headers);
}

it('recusa webhook sem secret válido', function (): void {
    webhook(['event' => 'online', 'path' => 'live'], secret: 'errado')->assertForbidden();
    webhook(['event' => 'online', 'path' => 'live'], secret: null)->assertForbidden();
});

it('marca a live corrente como no ar quando o sinal fica online', function (): void {
    Event::fake([LiveStarted::class]);
    $live = Live::factory()->create();

    webhook(['event' => 'online', 'path' => 'live'])->assertNoContent();

    expect($live->refresh()->status)->toBe(LiveStatus::OnAir)
        ->and($live->started_at)->not->toBeNull();
    Event::assertDispatched(LiveStarted::class);
});

it('ignora online sem live corrente', function (): void {
    Event::fake([LiveStarted::class]);
    Live::factory()->ended()->create();

    webhook(['event' => 'online', 'path' => 'live'])->assertNoContent();

    Event::assertNotDispatched(LiveStarted::class);
});

it('aceita e ignora o evento offline', function (): void {
    $live = Live::factory()->onAir()->create();

    webhook(['event' => 'offline', 'path' => 'live'])->assertNoContent();

    expect($live->refresh()->status)->toBe(LiveStatus::OnAir);
});
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/IngestWebhookTest.php`
Expected: FAIL (404).

- [ ] **Step 4: Controller + rota do webhook**

`src/IngestWebhookController.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live;

use He4rt\Live\Actions\MarkLiveOnline;
use He4rt\Live\Models\Live;
use Illuminate\Http\Request;
use Illuminate\Http\Response;

/** Recebe os hooks runOnOnline/runOnOffline do mediamtx. */
final class IngestWebhookController
{
    public function __invoke(Request $request, MarkLiveOnline $markOnline): Response
    {
        $secret = config()->string('live.webhook_secret');

        abort_unless(
            $secret !== '' && hash_equals($secret, (string) $request->header('X-Live-Webhook-Secret')),
            Response::HTTP_FORBIDDEN,
        );

        if ($request->json('event') === 'online') {
            $live = Live::query()->current()->first();

            if ($live !== null) {
                $markOnline->execute($live);
            }
        }

        return response()->noContent();
    }
}
```

Rota em `routes/live-routes.php`: `Route::post('live/ingest/webhook', IngestWebhookController::class)->name('live.ingest-webhook');` — conferir em `bootstrap/app.php` como `live/ingest/auth` trata CSRF/middleware e replicar para `live/ingest/webhook`.

Run: `./vendor/bin/pest app-modules/live/tests/Feature/IngestWebhookTest.php`
Expected: PASS.

- [ ] **Step 5: Rename `LiveStatus` DTO → `StreamStatus` + cache de 5s**

1. `git mv app-modules/live/src/DTOs/LiveStatus.php app-modules/live/src/DTOs/StreamStatus.php`, renomear classe/usos (`CheckLiveStatusAction`, testes). O docstring passa a "Estado atual do stream segundo a Control API do mediamtx."
2. Em `CheckLiveStatusAction::execute()`, envolver o corpo atual em cache:

```php
public function execute(): StreamStatus
{
    return Cache::remember(
        'live:stream-status',
        now()->addSeconds(5),
        fn (): StreamStatus => $this->fetch(),
    );
}
```

(`fetch()` = corpo atual; import `Illuminate\Support\Facades\Cache`.)

3. Em `CheckLiveStatusActionTest.php`, ajustar o nome do DTO e adicionar:

```php
it('cacheia o status por alguns segundos', function (): void {
    Http::fake(['localhost:9997/*' => Http::response(['ready' => true, 'readyTime' => '2026-08-29T20:00:00Z'])]);

    resolve(CheckLiveStatusAction::class)->execute();
    resolve(CheckLiveStatusAction::class)->execute();

    Http::assertSentCount(1);
});
```

Run: `./vendor/bin/pest app-modules/live/tests`
Expected: PASS (suíte inteira do módulo).

- [ ] **Step 6: Suíte do portal (consome o action renomeado), checks e commit**

Run: `./vendor/bin/pest app-modules/portal/tests/Feature/LivePageTest.php`
Expected: PASS (a view usa `$status->onAir`, sem referência ao nome da classe; se falhar, ajustar imports no portal).
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/live/src --ansi`

```bash
git add app-modules/live bootstrap/app.php app-modules/portal
git commit -m 'feat(live): ingest contra o banco, webhook de sinal e cache do status'
```

---

### Task 4: Activity — `ModerationType::MessageDeleted` e filtro de provider no `DiscordSource`

**Files:**

- Modify: `app-modules/activity/src/Moderation/Enums/ModerationType.php`
- Modify: `app-modules/activity/src/Retrospective/DiscordSource.php` (método `messages()` ~linha 394 e subqueries keyed por identidade)
- Test: Create `app-modules/activity/tests/Feature/Retrospective/DiscordSourceProviderFilterTest.php`

**Interfaces:**

- Consumes (Task 1): `IdentityProvider::He4rtLives`.
- Produces: `ModerationType::MessageDeleted` (`'message_deleted'`, contratos Filament preenchidos); `DiscordSource` contando apenas mensagens cuja `ExternalIdentity` tem `provider = Discord`.

- [ ] **Step 1: Teste que falha**

Conferir os testes existentes de `DiscordSource` (em `app-modules/activity/tests/`) para reusar helpers/factories de retrospectiva. Novo teste (adaptar o setup ao padrão dos vizinhos — o período/filters mínimos que os testes existentes usam):

```php
<?php

declare(strict_types=1);

use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\ExternalIdentity\Enums\IdentityProvider;
use He4rt\Identity\ExternalIdentity\Models\ExternalIdentity;

it('não conta mensagens do chat da live na retrospectiva do Discord', function (): void {
    $discordIdentity = ExternalIdentity::factory()->create(['provider' => IdentityProvider::Discord]);
    $liveIdentity = ExternalIdentity::factory()->create(['provider' => IdentityProvider::He4rtLives]);

    Message::factory()->for($discordIdentity, 'provider')->create(['sent_at' => now()]);
    Message::factory()->for($liveIdentity, 'provider')->create(['sent_at' => now()]);

    $count = /* invocar DiscordSource::messages() (ou o método público que o expõe)
                no período corrente, seguindo o padrão dos testes existentes */;

    expect($count)->toBe(1);
});
```

**Atenção:** o snippet acima marca o ponto de invocação — o implementador DEVE olhar os testes existentes de `DiscordSource` e usar a mesma forma de instanciar/chamar (período, filters). Se `messages()` for privado, testar pelo método público que o consome (o de contagem de mensagens da retrô).

Run: `./vendor/bin/pest app-modules/activity/tests/Feature/Retrospective/DiscordSourceProviderFilterTest.php`
Expected: FAIL (count = 2).

- [ ] **Step 2: Filtro no `DiscordSource` + caso novo no enum**

Em `DiscordSource::messages()` (a query base de `Message::query()` ~linha 394), adicionar:

```php
->whereHas('provider', fn (Builder $query) => $query->where('provider', IdentityProvider::Discord))
```

Varrer o arquivo por outras queries que partem de `Message::query()` ou `MembershipEvent`/`ModerationEvent`/`Reaction` sem passar por `messages()` e aplicar o mesmo filtro onde a fonte é "mensagem/identidade do Discord" (as subqueries de reações herdam via `whereIn(..., $this->messages(...))` — essas não precisam).

Em `ModerationType`, adicionar `case MessageDeleted = 'message_deleted';` e preencher TODOS os `match` (label `'Mensagem apagada'`, cor/ícone/descrição coerentes com os casos existentes, sem `default`).

- [ ] **Step 3: Testes, checks e commit**

Run: `./vendor/bin/pest app-modules/activity/tests`
Expected: PASS (inclusive os testes existentes de retrospectiva — se algum quebrar por dados sem identidade Discord, corrigir o SETUP do teste, não o filtro).
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/activity/src --ansi`

```bash
git add app-modules/activity
git commit -m 'fix(activity): retrospectiva do Discord filtra provider e enum de moderação ganha MessageDeleted'
```

---

### Task 5: Chat de domínio — `SendChatMessage` e `DeleteChatMessage` (módulo live)

**Files:**

- Modify: `app-modules/live/composer.json` (require `"he4rt/activity": "^1.0.0"`, `"he4rt/identity": "^1.0.0"`)
- Create: `app-modules/live/src/Chat/DTOs/ChatMessageData.php`
- Create: `app-modules/live/src/Chat/Actions/ResolveChatIdentity.php`
- Create: `app-modules/live/src/Chat/Actions/SendChatMessage.php`
- Create: `app-modules/live/src/Chat/Actions/DeleteChatMessage.php`
- Create: `app-modules/live/src/Chat/Exceptions/ChatMessageRejected.php`
- Create: `app-modules/live/src/Events/ChatMessageSent.php`
- Create: `app-modules/live/src/Events/ChatMessageDeleted.php`
- Test: `app-modules/live/tests/Feature/Chat/SendChatMessageTest.php`, `app-modules/live/tests/Feature/Chat/DeleteChatMessageTest.php`

**Interfaces:**

- Consumes: `IdentityProvider::He4rtLives` (Task 1); `Live` (Task 2); `ResolveExternalIdentity::handle(ResolveUserProviderDTO): ExternalIdentity` e `ResolveUserProviderDTO::make(array)` (identity, existentes); `Message` (activity); `ModerationType::MessageDeleted` (Task 4).
- Produces:
    - `ChatMessageData` (`final readonly`: `string $id`, `string $authorName`, `string $authorUsername`, `string $authorAvatarUrl`, `string $content`, `CarbonInterface $sentAt`; `::fromMessage(Message $message, User $author): self`; `toArray(): array`).
    - `SendChatMessage::execute(User $user, Live $live, string $content): ChatMessageData` — lança `ChatMessageRejected` (banido/suspenso, rate limit, live encerrada); dispara `ChatMessageSent`.
    - `DeleteChatMessage::execute(Message $message, User $moderator): void` — hard delete + `ModerationEvent` + `ChatMessageDeleted`.
    - Eventos `ChatMessageSent` (`public string $liveId`, `public array $message` = `ChatMessageData->toArray()`, broadcastAs `'ChatMessageSent'`) e `ChatMessageDeleted` (`public string $liveId`, `public string $messageId`, broadcastAs `'ChatMessageDeleted'`), ambos `ShouldBroadcastNow` no canal `live.{liveId}`.

- [ ] **Step 1: Dependências do módulo**

Adicionar em `app-modules/live/composer.json` → `require`: `"he4rt/activity": "^1.0.0"` e `"he4rt/identity": "^1.0.0"` (ordem alfabética). Run: `composer update he4rt/live --no-interaction` (path repos com symlink; se nada a fazer, seguir).

- [ ] **Step 2: Testes que falham — envio**

`app-modules/live/tests/Feature/Chat/SendChatMessageTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Activity\Message\Enums\MessageKind;
use He4rt\Activity\Message\Enums\MessageSourceKind;
use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\ExternalIdentity\Enums\IdentityProvider;
use He4rt\Identity\ExternalIdentity\Models\ExternalIdentity;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Chat\Actions\SendChatMessage;
use He4rt\Live\Chat\Exceptions\ChatMessageRejected;
use He4rt\Live\Events\ChatMessageSent;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\Event;
use Illuminate\Support\Facades\RateLimiter;

beforeEach(function (): void {
    Event::fake([ChatMessageSent::class]);
});

it('persiste a mensagem no pipeline de atividade com identidade He4rtLives', function (): void {
    $user = User::factory()->create();
    $live = Live::factory()->onAir()->create();

    $data = resolve(SendChatMessage::class)->execute($user, $live, 'salve chat!');

    $message = Message::query()->sole();
    $identity = ExternalIdentity::query()->where('provider', IdentityProvider::He4rtLives)->sole();

    expect($message->external_identity_id)->toBe($identity->id)
        ->and($identity->model_id)->toBe($user->id)
        ->and($identity->external_account_id)->toBe($user->id)
        ->and($message->channel_id)->toBe($live->id)
        ->and($message->content)->toBe('salve chat!')
        ->and($message->obtained_experience)->toBe(0)
        ->and($message->kind)->toBe(MessageKind::Default)
        ->and($message->source_kind)->toBe(MessageSourceKind::User)
        ->and($data->content)->toBe('salve chat!');
    Event::assertDispatched(ChatMessageSent::class, fn (ChatMessageSent $e): bool => $e->liveId === $live->id);
});

it('reusa a identidade He4rtLives em envios seguintes', function (): void {
    $user = User::factory()->create();
    $live = Live::factory()->onAir()->create();
    $action = resolve(SendChatMessage::class);

    $action->execute($user, $live, 'primeira');
    $action->execute($user, $live, 'segunda');

    expect(ExternalIdentity::query()->where('provider', IdentityProvider::He4rtLives)->count())->toBe(1);
});

it('rejeita usuário banido', function (): void {
    $user = User::factory()->create(['banned_at' => now()]);
    $live = Live::factory()->onAir()->create();

    resolve(SendChatMessage::class)->execute($user, $live, 'oi');
})->throws(ChatMessageRejected::class);

it('rejeita usuário suspenso', function (): void {
    $user = User::factory()->create(['suspended_until' => now()->addDay()]);
    $live = Live::factory()->onAir()->create();

    resolve(SendChatMessage::class)->execute($user, $live, 'oi');
})->throws(ChatMessageRejected::class);

it('rejeita envio em live encerrada', function (): void {
    $user = User::factory()->create();
    $live = Live::factory()->ended()->create();

    resolve(SendChatMessage::class)->execute($user, $live, 'oi');
})->throws(ChatMessageRejected::class);

it('aplica rate limit de 5 mensagens por 10 segundos', function (): void {
    $user = User::factory()->create();
    $live = Live::factory()->onAir()->create();
    RateLimiter::clear('live-chat:'.$user->id);
    $action = resolve(SendChatMessage::class);

    foreach (range(1, 5) as $i) {
        $action->execute($user, $live, "mensagem {$i}");
    }

    $action->execute($user, $live, 'mensagem 6');
})->throws(ChatMessageRejected::class);
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/Chat/SendChatMessageTest.php`
Expected: FAIL.

Conferir a `UserFactory` (identity) para os states corretos de banido/suspenso — se existirem states (`banned()`, `suspended()`), usar os states em vez de atributos crus.

- [ ] **Step 3: Implementar DTO, identidade, envio e eventos**

`src/Chat/DTOs/ChatMessageData.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Chat\DTOs;

use Carbon\CarbonInterface;
use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\User\Models\User;

/** Projeção pública de uma mensagem do chat, usada no broadcast e na renderização. */
final readonly class ChatMessageData
{
    public function __construct(
        public string $id,
        public string $authorName,
        public string $authorUsername,
        public string $authorAvatarUrl,
        public string $content,
        public CarbonInterface $sentAt,
    ) {}

    public static function fromMessage(Message $message, User $author): self
    {
        return new self(
            id: $message->id,
            authorName: $author->name,
            authorUsername: $author->username,
            authorAvatarUrl: $author->getFilamentAvatarUrl(),
            content: $message->content,
            sentAt: $message->sent_at ?? $message->created_at ?? now(),
        );
    }

    /** @return array<string, string> */
    public function toArray(): array
    {
        return [
            'id' => $this->id,
            'authorName' => $this->authorName,
            'authorUsername' => $this->authorUsername,
            'authorAvatarUrl' => $this->authorAvatarUrl,
            'content' => $this->content,
            'sentAt' => $this->sentAt->toIso8601String(),
        ];
    }
}
```

`src/Chat/Exceptions/ChatMessageRejected.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Chat\Exceptions;

use DomainException;

final class ChatMessageRejected extends DomainException
{
    public static function userBlocked(): self
    {
        return new self('Sua conta não pode enviar mensagens no chat.');
    }

    public static function liveNotAcceptingMessages(): self
    {
        return new self('Esta live não está mais recebendo mensagens.');
    }

    public static function rateLimited(int $seconds): self
    {
        return new self("Calma! Aguarde {$seconds}s para enviar de novo.");
    }
}
```

`src/Chat/Actions/ResolveChatIdentity.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Chat\Actions;

use He4rt\Identity\ExternalIdentity\Actions\ResolveExternalIdentity;
use He4rt\Identity\ExternalIdentity\DTOs\ResolveUserProviderDTO;
use He4rt\Identity\ExternalIdentity\Enums\IdentityProvider;
use He4rt\Identity\ExternalIdentity\Models\ExternalIdentity;
use He4rt\Identity\User\Models\User;

/** Garante a identidade sintética He4rtLives do usuário (find-or-create). */
final readonly class ResolveChatIdentity
{
    public function __construct(private ResolveExternalIdentity $resolveExternalIdentity) {}

    public function execute(User $user): ExternalIdentity
    {
        $identity = $this->resolveExternalIdentity->handle(ResolveUserProviderDTO::make([
            'provider' => IdentityProvider::He4rtLives,
            'external_account_id' => $user->id,
            'model_type' => $user->getMorphClass(),
            'username' => $user->username,
        ]));

        if ($identity->model_id !== $user->id) {
            $identity->update(['model_id' => $user->id]);
        }

        return $identity;
    }
}
```

**Nota ao implementador:** conferir a assinatura real de `ResolveUserProviderDTO::make()` (campos aceitos, se `model_id` entra no create) em `app-modules/identity/src/ExternalIdentity/DTOs/`. Se o `firstOrCreate` de `ResolveExternalIdentity` não setar `model_id`, o ajuste pós-create acima cobre; se setar, remover o `if`.

`src/Chat/Actions/SendChatMessage.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Chat\Actions;

use He4rt\Activity\Message\Enums\MessageKind;
use He4rt\Activity\Message\Enums\MessageSourceKind;
use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\User\Enums\UserSituation;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Chat\DTOs\ChatMessageData;
use He4rt\Live\Chat\Exceptions\ChatMessageRejected;
use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Events\ChatMessageSent;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\RateLimiter;

/** Envia uma mensagem ao chat da live, persistindo no pipeline de atividade. */
final readonly class SendChatMessage
{
    private const int MAX_MESSAGES = 5;

    private const int DECAY_SECONDS = 10;

    public function __construct(private ResolveChatIdentity $resolveChatIdentity) {}

    public function execute(User $user, Live $live, string $content): ChatMessageData
    {
        if ($user->situation !== UserSituation::Active) {
            throw ChatMessageRejected::userBlocked();
        }

        if ($live->status === LiveStatus::Ended) {
            throw ChatMessageRejected::liveNotAcceptingMessages();
        }

        $rateLimitKey = 'live-chat:'.$user->id;

        if (RateLimiter::tooManyAttempts($rateLimitKey, self::MAX_MESSAGES)) {
            throw ChatMessageRejected::rateLimited(RateLimiter::availableIn($rateLimitKey));
        }

        RateLimiter::hit($rateLimitKey, self::DECAY_SECONDS);

        $identity = $this->resolveChatIdentity->execute($user);

        $message = Message::query()->create([
            'external_identity_id' => $identity->id,
            'channel_id' => $live->id,
            'content' => $content,
            'obtained_experience' => 0,
            'kind' => MessageKind::Default,
            'source_kind' => MessageSourceKind::User,
            'sent_at' => now(),
        ]);

        $data = ChatMessageData::fromMessage($message, $user);

        ChatMessageSent::dispatch($live->id, $data->toArray());

        return $data;
    }
}
```

`src/Events/ChatMessageSent.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Events;

use Illuminate\Broadcasting\Channel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcastNow;
use Illuminate\Foundation\Events\Dispatchable;

/** Nova mensagem no chat da live. */
final class ChatMessageSent implements ShouldBroadcastNow
{
    use Dispatchable;

    /** @param array<string, string> $message */
    public function __construct(
        public string $liveId,
        public array $message,
    ) {}

    public function broadcastOn(): Channel
    {
        return new Channel('live.'.$this->liveId);
    }

    public function broadcastAs(): string
    {
        return 'ChatMessageSent';
    }
}
```

`src/Events/ChatMessageDeleted.php` — mesmo formato com `public string $liveId, public string $messageId` e `broadcastAs(): 'ChatMessageDeleted'`.

Run: `./vendor/bin/pest app-modules/live/tests/Feature/Chat/SendChatMessageTest.php`
Expected: PASS.

- [ ] **Step 4: Teste que falha — moderação**

`app-modules/live/tests/Feature/Chat/DeleteChatMessageTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Activity\Message\Models\Message;
use He4rt\Activity\Moderation\Enums\ModerationType;
use He4rt\Activity\Moderation\Models\ModerationEvent;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Chat\Actions\DeleteChatMessage;
use He4rt\Live\Chat\Actions\SendChatMessage;
use He4rt\Live\Events\ChatMessageDeleted;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\Event;

it('apaga a mensagem, registra moderação e broadcasta a remoção', function (): void {
    Event::fake([ChatMessageDeleted::class]);
    $author = User::factory()->create();
    $moderator = User::factory()->create();
    $live = Live::factory()->onAir()->create();
    $data = resolve(SendChatMessage::class)->execute($author, $live, 'mensagem imprópria');
    $message = Message::query()->sole();

    resolve(DeleteChatMessage::class)->execute($message, $moderator);

    $event = ModerationEvent::query()->sole();

    expect(Message::query()->count())->toBe(0)
        ->and($event->type)->toBe(ModerationType::MessageDeleted)
        ->and($event->metadata)->toMatchArray(['content' => 'mensagem imprópria', 'live_id' => $live->id]);
    Event::assertDispatched(
        ChatMessageDeleted::class,
        fn (ChatMessageDeleted $e): bool => $e->messageId === $data->id && $e->liveId === $live->id,
    );
});
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/Chat/DeleteChatMessageTest.php`
Expected: FAIL.

- [ ] **Step 5: Implementar `DeleteChatMessage`**

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Chat\Actions;

use He4rt\Activity\Message\Models\Message;
use He4rt\Activity\Moderation\Enums\ModerationType;
use He4rt\Activity\Moderation\Models\ModerationEvent;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Events\ChatMessageDeleted;
use Illuminate\Support\Facades\DB;

/** Remove uma mensagem do chat com trilha de moderação e aviso em tempo real. */
final readonly class DeleteChatMessage
{
    public function __construct(private ResolveChatIdentity $resolveChatIdentity) {}

    public function execute(Message $message, User $moderator): void
    {
        $liveId = (string) $message->channel_id;
        $messageId = $message->id;
        $moderatorIdentity = $this->resolveChatIdentity->execute($moderator);

        DB::transaction(function () use ($message, $moderatorIdentity, $liveId): void {
            ModerationEvent::query()->create([
                'external_identity_id' => $message->external_identity_id,
                'moderator_identity_id' => $moderatorIdentity->id,
                'type' => ModerationType::MessageDeleted,
                'metadata' => ['content' => $message->content, 'live_id' => $liveId, 'message_id' => $message->id],
                'occurred_at' => now(),
            ]);

            $message->delete();
        });

        ChatMessageDeleted::dispatch($liveId, $messageId);
    }
}
```

**Nota:** NÃO preencher `source_message_id` (FK para a própria mensagem que será deletada); o snapshot vive em `metadata`.

Run: `./vendor/bin/pest app-modules/live/tests/Feature/Chat`
Expected: PASS.

- [ ] **Step 6: Checks e commit**

Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/live/src --ansi`

```bash
git add app-modules/live composer.lock
git commit -m 'feat(live): chat de domínio persistido no pipeline de atividade'
```

---

### Task 6: Audiência — heartbeat, pico e amostras

**Files:**

- Create (artisan): migration `create_live_viewer_samples_table` (`--module=live`)
- Create: `app-modules/live/src/Contracts/ViewerPresenceContract.php`
- Create: `app-modules/live/src/Audience/RedisViewerPresence.php`
- Create: `app-modules/live/src/Audience/InMemoryViewerPresence.php`
- Create: `app-modules/live/src/Audience/Actions/RecordViewerHeartbeat.php`
- Create: `app-modules/live/src/Audience/Actions/CountViewers.php`
- Create: `app-modules/live/src/Models/LiveViewerSample.php`
- Create: `app-modules/live/database/factories/LiveViewerSampleFactory.php`
- Create: `app-modules/live/src/Console/SampleLiveViewersCommand.php`
- Modify: `app-modules/live/src/LiveServiceProvider.php` (binding do contract + registro do command)
- Modify: `routes/console.php` (schedule)
- Test: `app-modules/live/tests/Feature/Audience/RecordViewerHeartbeatTest.php`, `app-modules/live/tests/Feature/Audience/SampleLiveViewersCommandTest.php`

**Interfaces:**

- Consumes (Task 2): `Live` (+ factory, `peak_viewers`), `LiveStatus`.
- Produces:
    - `ViewerPresenceContract` — `touch(string $liveId, string $visitorId): void`; `countActive(string $liveId): int` (janela de 30s).
    - `RecordViewerHeartbeat::execute(Live $live, string $visitorId): int` — registra presença, atualiza `peak_viewers` se superado, retorna espectadores atuais.
    - `CountViewers::execute(Live $live): int`.
    - Model `LiveViewerSample` (`@property string $id, string $live_id, int $viewers, CarbonInterface $sampled_at`).
    - Command `live:sample-viewers` (agendado a cada minuto).
    - Binding: `ViewerPresenceContract` → `RedisViewerPresence` (produção) / testes fazem swap p/ `InMemoryViewerPresence` via `$this->app->bind`.

- [ ] **Step 1: Migration**

Run: `php artisan make:migration create_live_viewer_samples_table --module=live --no-interaction`

```php
Schema::create('live_viewer_samples', static function (Blueprint $table): void {
    $table->uuid('id')->primary();
    $table->foreignUuid('live_id')->constrained('lives')->cascadeOnDelete();
    $table->integer('viewers');
    $table->timestampTz('sampled_at');
    $table->timestampsTz();
    $table->index(['live_id', 'sampled_at']);
});
```

- [ ] **Step 2: Testes que falham**

`RecordViewerHeartbeatTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Live\Audience\Actions\RecordViewerHeartbeat;
use He4rt\Live\Audience\InMemoryViewerPresence;
use He4rt\Live\Contracts\ViewerPresenceContract;
use He4rt\Live\Models\Live;

beforeEach(function (): void {
    $this->app->singleton(ViewerPresenceContract::class, InMemoryViewerPresence::class);
});

it('conta espectadores distintos na janela ativa', function (): void {
    $live = Live::factory()->onAir()->create();
    $action = resolve(RecordViewerHeartbeat::class);

    $action->execute($live, 'sessao-a');
    $action->execute($live, 'sessao-a');
    $viewers = $action->execute($live, 'sessao-b');

    expect($viewers)->toBe(2);
});

it('atualiza o pico apenas quando superado', function (): void {
    $live = Live::factory()->onAir()->create(['peak_viewers' => 5]);
    $action = resolve(RecordViewerHeartbeat::class);

    $action->execute($live, 'sessao-a');

    expect($live->refresh()->peak_viewers)->toBe(5);

    foreach (range(1, 6) as $i) {
        $action->execute($live, "sessao-{$i}");
    }

    expect($live->refresh()->peak_viewers)->toBe(6);
});

it('expira presenças fora da janela de 30 segundos', function (): void {
    $live = Live::factory()->onAir()->create();
    $action = resolve(RecordViewerHeartbeat::class);

    $action->execute($live, 'sessao-antiga');
    $this->travel(31)->seconds();

    expect($action->execute($live, 'sessao-nova'))->toBe(1);
});
```

`SampleLiveViewersCommandTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Live\Audience\InMemoryViewerPresence;
use He4rt\Live\Contracts\ViewerPresenceContract;
use He4rt\Live\Models\Live;
use He4rt\Live\Models\LiveViewerSample;

use function Pest\Laravel\artisan;

beforeEach(function (): void {
    $this->app->singleton(ViewerPresenceContract::class, InMemoryViewerPresence::class);
});

it('grava uma amostra para a live no ar', function (): void {
    $live = Live::factory()->onAir()->create();
    resolve(ViewerPresenceContract::class)->touch($live->id, 'sessao-a');

    artisan('live:sample-viewers')->assertSuccessful();

    $sample = LiveViewerSample::query()->sole();
    expect($sample->live_id)->toBe($live->id)->and($sample->viewers)->toBe(1);
});

it('não grava amostra sem live no ar', function (): void {
    Live::factory()->create();

    artisan('live:sample-viewers')->assertSuccessful();

    expect(LiveViewerSample::query()->count())->toBe(0);
});
```

Run: `./vendor/bin/pest app-modules/live/tests/Feature/Audience`
Expected: FAIL.

- [ ] **Step 3: Implementar contract e as duas implementações**

`src/Contracts/ViewerPresenceContract.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Contracts;

interface ViewerPresenceContract
{
    public function touch(string $liveId, string $visitorId): void;

    /** Espectadores com heartbeat nos últimos 30 segundos. */
    public function countActive(string $liveId): int;
}
```

`src/Audience/RedisViewerPresence.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Audience;

use He4rt\Live\Contracts\ViewerPresenceContract;
use Illuminate\Support\Facades\Redis;

/** Presença em sorted set no Redis: score = timestamp do último heartbeat. */
final readonly class RedisViewerPresence implements ViewerPresenceContract
{
    private const int WINDOW_SECONDS = 30;

    public function touch(string $liveId, string $visitorId): void
    {
        Redis::connection()->zadd($this->key($liveId), now()->timestamp, $visitorId);
    }

    public function countActive(string $liveId): int
    {
        $connection = Redis::connection();
        $cutoff = now()->timestamp - self::WINDOW_SECONDS;

        $connection->zremrangebyscore($this->key($liveId), '-inf', (string) $cutoff);

        return (int) $connection->zcard($this->key($liveId));
    }

    private function key(string $liveId): string
    {
        return 'live:viewers:'.$liveId;
    }
}
```

`src/Audience/InMemoryViewerPresence.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Audience;

use He4rt\Live\Contracts\ViewerPresenceContract;

/** Presença em memória para testes e ambientes sem Redis. */
final class InMemoryViewerPresence implements ViewerPresenceContract
{
    private const int WINDOW_SECONDS = 30;

    /** @var array<string, array<string, int>> */
    private array $heartbeats = [];

    public function touch(string $liveId, string $visitorId): void
    {
        $this->heartbeats[$liveId][$visitorId] = now()->timestamp;
    }

    public function countActive(string $liveId): int
    {
        $cutoff = now()->timestamp - self::WINDOW_SECONDS;

        $this->heartbeats[$liveId] = array_filter(
            $this->heartbeats[$liveId] ?? [],
            static fn (int $timestamp): bool => $timestamp > $cutoff,
        );

        return count($this->heartbeats[$liveId]);
    }
}
```

No `LiveServiceProvider::register()`: `$this->app->singleton(ViewerPresenceContract::class, RedisViewerPresence::class);`

- [ ] **Step 4: Actions, model da amostra e command**

`src/Audience/Actions/CountViewers.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Audience\Actions;

use He4rt\Live\Contracts\ViewerPresenceContract;
use He4rt\Live\Models\Live;

/** Conta os espectadores ativos da live. */
final readonly class CountViewers
{
    public function __construct(private ViewerPresenceContract $presence) {}

    public function execute(Live $live): int
    {
        return $this->presence->countActive($live->id);
    }
}
```

`src/Audience/Actions/RecordViewerHeartbeat.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Audience\Actions;

use He4rt\Live\Contracts\ViewerPresenceContract;
use He4rt\Live\Models\Live;

/** Registra o "estou assistindo" de uma aba e devolve a audiência atual. */
final readonly class RecordViewerHeartbeat
{
    public function __construct(private ViewerPresenceContract $presence) {}

    public function execute(Live $live, string $visitorId): int
    {
        $this->presence->touch($live->id, $visitorId);

        $viewers = $this->presence->countActive($live->id);

        if ($viewers > $live->peak_viewers) {
            $live->update(['peak_viewers' => $viewers]);
        }

        return $viewers;
    }
}
```

`src/Models/LiveViewerSample.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Models;

use Carbon\CarbonInterface;
use He4rt\Live\Database\Factories\LiveViewerSampleFactory;
use Illuminate\Database\Eloquent\Attributes\Table;
use Illuminate\Database\Eloquent\Attributes\UseFactory;
use Illuminate\Database\Eloquent\Concerns\HasUuids;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

/**
 * @property string $id
 * @property string $live_id
 * @property int $viewers
 * @property CarbonInterface $sampled_at
 * @property CarbonInterface|null $created_at
 * @property CarbonInterface|null $updated_at
 */
#[UseFactory(factoryClass: LiveViewerSampleFactory::class)]
#[Table(name: 'live_viewer_samples')]
final class LiveViewerSample extends Model
{
    /** @use HasFactory<LiveViewerSampleFactory> */
    use HasFactory;
    use HasUuids;

    /** @return BelongsTo<Live, $this> */
    public function live(): BelongsTo
    {
        return $this->belongsTo(Live::class);
    }

    /** @return array<string, mixed> */
    protected function casts(): array
    {
        return ['sampled_at' => 'datetime'];
    }
}
```

`LiveViewerSampleFactory` (definition: `live_id => Live::factory()`, `viewers => fake()->numberBetween(0, 100)`, `sampled_at => now()`).

`src/Console/SampleLiveViewersCommand.php`:

```php
<?php

declare(strict_types=1);

namespace He4rt\Live\Console;

use He4rt\Live\Audience\Actions\CountViewers;
use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Models\Live;
use He4rt\Live\Models\LiveViewerSample;
use Illuminate\Console\Command;

/** Grava a audiência da live no ar para a série temporal do admin. */
final class SampleLiveViewersCommand extends Command
{
    protected $signature = 'live:sample-viewers';

    protected $description = 'Grava uma amostra de audiência da live no ar';

    public function handle(CountViewers $countViewers): int
    {
        $live = Live::query()->where('status', LiveStatus::OnAir)->first();

        if ($live === null) {
            return self::SUCCESS;
        }

        LiveViewerSample::query()->create([
            'live_id' => $live->id,
            'viewers' => $countViewers->execute($live),
            'sampled_at' => now(),
        ]);

        return self::SUCCESS;
    }
}
```

Registrar no `LiveServiceProvider::boot()`: `$this->commands([SampleLiveViewersCommand::class]);` (se rodando no console). Em `routes/console.php`, seguindo o padrão do arquivo:

```php
Schedule::command('live:sample-viewers')->everyMinute();
```

- [ ] **Step 5: Testes, checks e commit**

Run: `./vendor/bin/pest app-modules/live/tests`
Expected: PASS.
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/live/src --ansi`

```bash
git add app-modules/live routes/console.php
git commit -m 'feat(live): audiência com heartbeat, pico e amostras por minuto'
```

---

### Task 7: Infra Reverb + Echo

**Files:**

- Modify: `composer.json` (require `laravel/reverb`; `reverb:start` no script `dev`), `composer.lock`
- Create/Modify: `config/reverb.php`, `config/broadcasting.php` (o que `reverb:install` gerar)
- Modify: `.env.example` (`BROADCAST_CONNECTION=reverb`, bloco `REVERB_*` + `VITE_REVERB_*`)
- Modify: `package.json` (`laravel-echo`, `pusher-js`)
- Create: `app-modules/portal/resources/js/echo.js`

**Interfaces:**

- Produces: `window.Echo` configurado (Reverb) exportado por `app-modules/portal/resources/js/echo.js` para import nos entries do portal; broadcasting default `reverb` em dev, testes continuam com o driver que o phpunit definir (verificar).

- [ ] **Step 1: Instalar Reverb**

Run: `composer require laravel/reverb --no-interaction`
Run: `php artisan reverb:install --no-interaction`
Depois: inspecionar o diff (`git status`/`git diff`) — o installer mexe em `config/broadcasting.php`, `.env`, `.env.example`, `routes/channels.php` (não deve remover o canal existente), `bootstrap/app.php` (broadcasting provider). Garantir que `.env.example` termine com `BROADCAST_CONNECTION=reverb` e o bloco `REVERB_APP_ID/KEY/SECRET/HOST/PORT/SCHEME` + espelhos `VITE_REVERB_APP_KEY/HOST/PORT/SCHEME`. Conferir `phpunit.xml`: se não houver `BROADCAST_CONNECTION`, adicionar `<env name="BROADCAST_CONNECTION" value="log"/>` para os testes não tentarem falar com Reverb.

- [ ] **Step 2: Echo no front do portal**

Run: `npm install laravel-echo pusher-js`

`app-modules/portal/resources/js/echo.js`:

```js
import Echo from 'laravel-echo';
import Pusher from 'pusher-js';

window.Pusher = Pusher;

export const echo = new Echo({
    broadcaster: 'reverb',
    key: import.meta.env.VITE_REVERB_APP_KEY,
    wsHost: import.meta.env.VITE_REVERB_HOST ?? window.location.hostname,
    wsPort: Number(import.meta.env.VITE_REVERB_PORT ?? 8080),
    wssPort: Number(import.meta.env.VITE_REVERB_PORT ?? 443),
    forceTLS: (import.meta.env.VITE_REVERB_SCHEME ?? 'http') === 'https',
    enabledTransports: ['ws', 'wss'],
});
```

- [ ] **Step 3: `composer dev` com Reverb**

No script `dev` do `composer.json`, adicionar `"php artisan reverb:start"` ao `concurrently` (uma cor e um name `reverb` a mais, espelhando o formato existente).

- [ ] **Step 4: Verificar e commitar**

Run: `npm run build`
Expected: build ok.
Run: `./vendor/bin/pest app-modules/live/tests --group=` (sanidade: suíte do live segue verde com o novo broadcasting default de teste)
Run: `php artisan about | grep -i broadcast`
Expected: driver configurado sem erro.

```bash
git add composer.json composer.lock package.json package-lock.json .env.example config bootstrap routes/channels.php phpunit.xml app-modules/portal/resources/js/echo.js
git commit -m 'chore(portal): laravel reverb e echo como infraestrutura de broadcast'
```

(Ajustar o `git add` ao que o installer realmente tocou.)

---

### Task 8: Portal — página `/live` com chat, heartbeat e player resiliente

**Files:**

- Modify: `app-modules/portal/src/Live/LivePage.php`
- Create: `app-modules/portal/src/Live/LiveChat.php`
- Modify: `app-modules/portal/resources/views/live.blade.php`
- Create: `app-modules/portal/resources/views/live-chat.blade.php`
- Modify: `app-modules/portal/resources/js/live-player.js`
- Modify: `app-modules/portal/composer.json` (nada novo — `he4rt/live` já existe; conferir)
- Test: Modify `app-modules/portal/tests/Feature/LivePageTest.php`; Create `app-modules/portal/tests/Feature/LiveChatTest.php`

**Interfaces:**

- Consumes: `Live::query()->current()` + `LiveStatus` (Task 2); `CheckLiveStatusAction::execute(): StreamStatus` (Task 3); `SendChatMessage`/`ChatMessageData`/`ChatMessageRejected` (Task 5); `RecordViewerHeartbeat`/`CountViewers` (Task 6); `echo.js` (Task 7); `Message` (activity) p/ histórico.
- Produces: UI final de `/live`.

**Desenho da página (evita morph destruindo o chat):** `LivePage` (pai) tem `wire:poll.10s="pulse"` — `pulse()` registra heartbeat (visitorId = `session()->getId()`) e re-renderiza player/contador. O chat é o componente aninhado `LiveChat` (filho não re-renderiza no morph do pai) e mantém a lista dentro de `wire:ignore`, com appends via JS/Echo.

- [ ] **Step 1: Ativar skills e testes que falham**

Implementador: ativar `livewire-specialist`, `tailwindcss-development` e `pest-testing` antes de codar.

Reescrever `LivePageTest.php` (manter os 2 testes atuais adaptados + novos):

```php
<?php

declare(strict_types=1);

use He4rt\Identity\User\Models\User;
use He4rt\Live\Audience\InMemoryViewerPresence;
use He4rt\Live\Contracts\ViewerPresenceContract;
use He4rt\Live\Models\Live;
use Illuminate\Support\Facades\Http;

use function Pest\Laravel\actingAs;
use function Pest\Laravel\get;

beforeEach(function (): void {
    $this->withoutVite();
    $this->app->singleton(ViewerPresenceContract::class, InMemoryViewerPresence::class);
    Http::fake(['localhost:9997/*' => Http::response(['ready' => false, 'readyTime' => null])]);
});

it('mostra o estado offline quando não há live corrente', function (): void {
    get('/live')
        ->assertOk()
        ->assertSee('Nenhuma live no ar agora')
        ->assertDontSee('data-live-player', escape: false);
});

it('mostra título, descrição e chat quando há live corrente', function (): void {
    Live::factory()->onAir()->create(['title' => 'Retrô de agosto', 'description' => 'Balanço do mês']);
    Http::fake(['localhost:9997/*' => Http::response(['ready' => true, 'readyTime' => '2026-08-29T20:00:00Z'])]);

    get('/live')
        ->assertOk()
        ->assertSee('Retrô de agosto')
        ->assertSee('Balanço do mês')
        ->assertSee('data-live-player', escape: false)
        ->assertSee('data-live-channel', escape: false);
});

it('mostra aguardando sinal quando a live existe mas o stream caiu', function (): void {
    Live::factory()->onAir()->create();

    get('/live')
        ->assertOk()
        ->assertSee('Aguardando sinal');
});

it('registra heartbeat e expõe o contador no poll', function (): void {
    $live = Live::factory()->onAir()->create();

    Livewire\Livewire::test(He4rt\Portal\Live\LivePage::class)
        ->call('pulse')
        ->assertSet('viewers', 1);

    expect(resolve(ViewerPresenceContract::class)->countActive($live->id))->toBe(1);
});
```

`LiveChatTest.php`:

```php
<?php

declare(strict_types=1);

use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Events\ChatMessageSent;
use He4rt\Live\Models\Live;
use He4rt\Portal\Live\LiveChat;
use Illuminate\Support\Facades\Event;

use function Pest\Livewire\livewire;

it('lista o histórico da live', function (): void {
    $live = Live::factory()->onAir()->create();
    $user = User::factory()->create();
    resolve(He4rt\Live\Chat\Actions\SendChatMessage::class)->execute($user, $live, 'primeira mensagem');

    livewire(LiveChat::class, ['liveId' => $live->id])
        ->assertSee('primeira mensagem')
        ->assertSee($user->username);
});

it('não mostra o form para visitante deslogado', function (): void {
    $live = Live::factory()->onAir()->create();

    livewire(LiveChat::class, ['liveId' => $live->id])
        ->assertSee('Entre para participar do chat')
        ->assertDontSeeHtml('wire:submit="send"');
});

it('usuário logado envia mensagem', function (): void {
    Event::fake([ChatMessageSent::class]);
    $live = Live::factory()->onAir()->create();
    $user = User::factory()->create();

    livewire(LiveChat::class, ['liveId' => $live->id])
        ->actingAs($user)
        ->set('draft', 'salve!')
        ->call('send')
        ->assertHasNoErrors()
        ->assertSet('draft', '');

    expect(Message::query()->where('content', 'salve!')->exists())->toBeTrue();
});

it('valida o tamanho da mensagem', function (): void {
    $live = Live::factory()->onAir()->create();
    $user = User::factory()->create();

    livewire(LiveChat::class, ['liveId' => $live->id])
        ->actingAs($user)
        ->set('draft', str_repeat('a', 501))
        ->call('send')
        ->assertHasErrors(['draft' => 'max']);
});

it('exibe erro amigável quando o domínio rejeita', function (): void {
    $live = Live::factory()->onAir()->create();
    $user = User::factory()->create(['banned_at' => now()]);

    livewire(LiveChat::class, ['liveId' => $live->id])
        ->actingAs($user)
        ->set('draft', 'oi')
        ->call('send')
        ->assertHasErrors('draft');
});
```

Run: `./vendor/bin/pest app-modules/portal/tests/Feature/LivePageTest.php app-modules/portal/tests/Feature/LiveChatTest.php`
Expected: FAIL.

- [ ] **Step 2: `LivePage` fase 2**

```php
<?php

declare(strict_types=1);

namespace He4rt\Portal\Live;

use He4rt\Live\Actions\CheckLiveStatusAction;
use He4rt\Live\Audience\Actions\RecordViewerHeartbeat;
use He4rt\Live\Models\Live;
use Illuminate\Contracts\View\View;
use Livewire\Attributes\Layout;
use Livewire\Component;

#[Layout(name: 'portal::components.layouts.app')]
final class LivePage extends Component
{
    public int $viewers = 0;

    public function pulse(): void
    {
        $live = Live::query()->current()->first();

        if ($live !== null) {
            $this->viewers = resolve(RecordViewerHeartbeat::class)->execute($live, session()->getId());
        }
    }

    public function render(CheckLiveStatusAction $status): View
    {
        $live = Live::query()->current()->first();

        return view('portal::live', [
            'live' => $live,
            'status' => $status->execute(),
        ]);
    }
}
```

- [ ] **Step 3: `LiveChat`**

```php
<?php

declare(strict_types=1);

namespace He4rt\Portal\Live;

use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Chat\Actions\SendChatMessage;
use He4rt\Live\Chat\DTOs\ChatMessageData;
use He4rt\Live\Chat\Exceptions\ChatMessageRejected;
use He4rt\Live\Models\Live;
use Illuminate\Contracts\View\View;
use Livewire\Attributes\Locked;
use Livewire\Component;

final class LiveChat extends Component
{
    #[Locked]
    public string $liveId;

    public string $draft = '';

    public function send(SendChatMessage $sendChatMessage): void
    {
        $this->validate(['draft' => ['required', 'string', 'min:1', 'max:500']]);

        $user = auth()->user();
        $live = Live::query()->findOrFail($this->liveId);

        abort_unless($user instanceof User, 403);

        try {
            $sendChatMessage->execute($user, $live, trim($this->draft));
        } catch (ChatMessageRejected $exception) {
            $this->addError('draft', $exception->getMessage());

            return;
        }

        $this->draft = '';
    }

    public function render(): View
    {
        $history = Message::query()
            ->where('channel_id', $this->liveId)
            ->orderByDesc('sent_at')
            ->limit(50)
            ->with('provider.user')
            ->get()
            ->reverse()
            ->values()
            ->map(fn (Message $message): array => ChatMessageData::fromMessage(
                $message,
                $message->provider->user ?? new User(['name' => 'Membro', 'username' => 'membro']),
            )->toArray());

        return view('portal::live-chat', ['history' => $history]);
    }
}
```

**Nota:** conferir a relação `ExternalIdentity::user()` (BelongsTo por `model_id`) — o eager load correto pode ser `provider.user`; validar com o teste. Mensagem enviada some do histórico ao ser deletada (hard delete) — nenhum tratamento extra.

- [ ] **Step 4: Views**

`live.blade.php` — evoluir a atual mantendo o design system (`hp-page`, tokens `text-text-high` etc.): grid `lg:grid-cols-[1fr_20rem]` com player à esquerda e chat à direita; header com `$live?->title`/`description` e badge; três estados: sem live corrente (placeholder atual), live corrente + `$status->onAir` (player com `data-live-player data-hls-url data-live-channel="{{ $live->id }}"`), live corrente sem sinal (`Aguardando sinal` + `data-live-channel` para o JS re-montar via Echo). `wire:poll.10s="pulse"` no wrapper (SEM condicional — heartbeat sempre). Contador: `<span>{{ $viewers }} assistindo</span>` quando > 0. Chat: `<livewire:portal.live-chat :live-id="$live->id" />` quando `$live !== null` (registrar alias do componente conforme o padrão do módulo portal — conferir como os componentes irmãos são registrados/nomeados).

`live-chat.blade.php` (estrutura essencial):

```blade
<section class="border-outline-low bg-elevation-01dp flex h-[32rem] flex-col rounded-lg border">
    <header class="border-outline-low border-b px-4 py-3">
        <h2 class="text-text-high text-sm font-semibold">Chat da live</h2>
    </header>

    <div wire:ignore data-chat-list class="flex-1 space-y-3 overflow-y-auto p-4">
        @foreach ($history as $message)
            <div data-chat-message data-message-id="{{ $message['id'] }}" class="flex items-start gap-2">
                <img src="{{ $message['authorAvatarUrl'] }}" alt="" class="h-6 w-6 rounded-full" />
                <p class="text-text-medium text-sm break-words">
                    <span class="text-text-high font-semibold">{{ $message['authorUsername'] }}</span>
                    {{ $message['content'] }}
                </p>
            </div>
        @endforeach
    </div>

    @auth
        <form wire:submit="send" class="border-outline-low flex items-center gap-2 border-t p-3">
            <input
                wire:model="draft"
                type="text"
                maxlength="500"
                placeholder="Mande sua mensagem"
                class="bg-elevation-02dp text-text-high w-full rounded-md px-3 py-2 text-sm"
            />
            <button type="submit" class="rounded-md bg-purple-600 px-3 py-2 text-sm font-semibold text-white">Enviar</button>
        </form>
        @error('draft')
            <p class="px-3 pb-3 text-xs text-red-400">{{ $message }}</p>
        @enderror
    @else
        <p class="border-outline-low text-text-medium border-t p-4 text-center text-sm">
            <a href="{{ route('login') }}" class="text-purple-400 underline">Entre</a> para participar do chat
        </p>
    @endauth
</section>
```

(Conferir o nome real da rota de login — `route('login')` pode ser outra no identity; usar a que o layout do portal já usa.)

- [ ] **Step 5: JS — Echo + player resiliente**

Reescrever `live-player.js`:

```js
import Hls from 'hls.js';
import { echo } from './echo';

let hls = null;
let subscribedChannel = null;

const teardown = () => {
    if (hls) {
        hls.destroy();
        hls = null;
    }
};

const showWaiting = () => {
    teardown();
    document.querySelector('[data-live-waiting]')?.classList.remove('hidden');
    document.querySelector('video[data-live-player]')?.classList.add('hidden');
};

const mountPlayer = () => {
    const video = document.querySelector('video[data-live-player]');
    if (!video) return;
    video.classList.remove('hidden');
    document.querySelector('[data-live-waiting]')?.classList.add('hidden');
    teardown();
    const src = video.dataset.hlsUrl;
    if (video.canPlayType('application/vnd.apple.mpegurl')) {
        video.src = src;
        video.play().catch(() => {});
        return;
    }
    if (Hls.isSupported()) {
        hls = new Hls();
        hls.on(Hls.Events.ERROR, (_event, data) => {
            if (data.fatal) showWaiting();
        });
        hls.loadSource(src);
        hls.attachMedia(video);
    }
};

const appendChatMessage = (message) => {
    const list = document.querySelector('[data-chat-list]');
    if (!list || list.querySelector(`[data-message-id="${CSS.escape(message.id)}"]`)) return;
    const item = document.createElement('div');
    item.dataset.chatMessage = '';
    item.dataset.messageId = message.id;
    item.className = 'flex items-start gap-2';
    const avatar = document.createElement('img');
    avatar.src = message.authorAvatarUrl;
    avatar.alt = '';
    avatar.className = 'h-6 w-6 rounded-full';
    const text = document.createElement('p');
    text.className = 'text-text-medium text-sm break-words';
    const author = document.createElement('span');
    author.className = 'text-text-high font-semibold';
    author.textContent = message.authorUsername;
    text.append(author, ` ${message.content}`);
    item.append(avatar, text);
    list.append(item);
    list.scrollTop = list.scrollHeight;
};

const subscribe = () => {
    const holder = document.querySelector('[data-live-channel]');
    if (!holder || subscribedChannel === holder.dataset.liveChannel) return;
    subscribedChannel = holder.dataset.liveChannel;
    echo.channel(`live.${subscribedChannel}`)
        .listen('.ChatMessageSent', (event) => appendChatMessage(event.message))
        .listen('.ChatMessageDeleted', (event) => {
            document.querySelector(`[data-message-id="${CSS.escape(event.messageId)}"]`)?.remove();
        })
        .listen('.LiveStarted', () => window.Livewire?.dispatch('$refresh') ?? mountPlayer())
        .listen('.LiveEnded', () => showWaiting());
};

const boot = () => {
    subscribe();
    mountPlayer();
};

document.addEventListener('DOMContentLoaded', boot);
document.addEventListener('livewire:init', () => {
    Livewire.hook('morphed', boot);
});
```

**Notas:** `.listen('.Nome')` com ponto = `broadcastAs`. `LiveStarted` → refresh do Livewire re-renderiza o pai com o player (o poll de status pode ainda dizer offline por até 5s de cache — aceitável; o `morphed` re-monta quando o player entrar no DOM). `data-live-waiting` é o wrapper do estado "Aguardando sinal" na view.

Run: `npm run build`
Expected: OK.

- [ ] **Step 6: Teste de regressão `@vite` multi-root**

Adicionar ao `LivePageTest.php` (motivo: no CI os assets são buildados antes do Pest, e a diretiva `@vite` fora do lugar já quebrou a página — ver histórico do MVP):

```php
it('carrega o entry do player pelo vite dentro do main', function (): void {
    Live::factory()->onAir()->create();

    $html = get('/live')->assertOk()->getContent();

    expect(substr_count($html, '<main'))->toBe(1);
});
```

(Validar contra o defeito real do MVP: a asserção deve reproduzir o sintoma da época — conferir o commit `7ad2965b` e ajustar a asserção para pegar exatamente aquela regressão: diretiva `@vite` dentro do `<main>` da página.)

- [ ] **Step 7: Testes, checks e commit**

Run: `./vendor/bin/pest app-modules/portal/tests`
Expected: PASS.
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/portal/src --ansi`

```bash
git add app-modules/portal
git commit -m 'feat(portal): chat em tempo real, heartbeat de audiência e player resiliente na página de live'
```

---

### Task 9: Panel-admin — LiveResource, moderação e gráfico de audiência

**Files:**

- Modify: `app-modules/panel-admin/composer.json` (`"he4rt/live": "^1.0.0"`)
- Modify: `app-modules/panel-admin/src/PanelAdminServiceProvider.php` (discover `Live/Resources`)
- Create: `app-modules/panel-admin/src/Live/Resources/LiveResource.php`
- Create: `app-modules/panel-admin/src/Live/Resources/LiveResource/Pages/ListLives.php`
- Create: `app-modules/panel-admin/src/Live/Resources/LiveResource/Pages/ViewLive.php`
- Create: `app-modules/panel-admin/src/Live/Resources/LiveResource/Widgets/LiveAudienceChart.php`
- Create: `app-modules/panel-admin/src/Live/Resources/LiveResource/Widgets/LiveChatMessages.php`
- Create: `docs/admin/en/system/lives.md` (KB do admin — grupo `system`)
- Test: `app-modules/panel-admin/tests/Feature/Live/LiveResourceTest.php`

**Interfaces:**

- Consumes: `Live`/`LiveStatus`/factories (Task 2); `CreateLive`, `EndLive`, `RotateStreamKey` (Task 2); `DeleteChatMessage` (Task 5); `LiveViewerSample` (Task 6); `Message` (activity).
- Produces: resource `lives` no painel admin.

- [ ] **Step 1: Preparação obrigatória**

Pesquisar com `search-docs` (packages: filament) antes de codar: "resource view page widgets", "chart widget", "table widget custom query", "action modal form". Espelhar convenções dos vizinhos (`ShortLinkResource`, `ModerationCaseResource`): navegação, labels, forma de registrar pages.

Adicionar `"he4rt/live": "^1.0.0"` ao composer.json do panel-admin. No `PanelAdminServiceProvider`, adicionar após o bloco Discord:

```php
->discoverResources(
    in: __DIR__.'/Live/Resources',
    for: 'He4rt\\PanelAdmin\\Live\\Resources',
)
```

- [ ] **Step 2: Testes que falham**

`LiveResourceTest.php` (seguir o padrão de autenticação dos testes irmãos do panel-admin — `actingAs` + acesso ao painel):

```php
<?php

declare(strict_types=1);

use Filament\Actions\Testing\TestAction;
use He4rt\Activity\Message\Models\Message;
use He4rt\Identity\User\Models\User;
use He4rt\Live\Enums\LiveStatus;
use He4rt\Live\Models\Live;
use He4rt\PanelAdmin\Live\Resources\LiveResource\Pages\ListLives;
use He4rt\PanelAdmin\Live\Resources\LiveResource\Pages\ViewLive;

use function Pest\Livewire\livewire;

beforeEach(function (): void {
    $this->actingAs(User::factory()->create());
    // Ajustar ao helper/panel bootstrapping usado pelos testes irmãos do panel-admin.
});

it('lista lives', function (): void {
    $lives = Live::factory()->count(2)->create(['status' => LiveStatus::Ended]);

    livewire(ListLives::class)->assertCanSeeTableRecords($lives);
});

it('cria live pela action da listagem', function (): void {
    livewire(ListLives::class)
        ->callAction('createLive', ['title' => 'Retrô de agosto', 'description' => 'Balanço'])
        ->assertNotified();

    expect(Live::query()->sole()->title)->toBe('Retrô de agosto');
});

it('bloqueia criação com live corrente aberta', function (): void {
    Live::factory()->create();

    livewire(ListLives::class)
        ->callAction('createLive', ['title' => 'Outra', 'description' => null])
        ->assertNotified();

    expect(Live::query()->count())->toBe(1);
});

it('encerra a live', function (): void {
    $live = Live::factory()->onAir()->create();

    livewire(ViewLive::class, ['record' => $live->id])
        ->callAction('endLive')
        ->assertNotified();

    expect($live->refresh()->status)->toBe(LiveStatus::Ended);
});

it('rotaciona a stream key', function (): void {
    $live = Live::factory()->create();
    $original = $live->stream_key;

    livewire(ViewLive::class, ['record' => $live->id])
        ->callAction('rotateStreamKey')
        ->assertNotified();

    expect($live->refresh()->stream_key)->not->toBe($original);
});

it('modera mensagem do chat a partir da view da live', function (): void {
    $live = Live::factory()->onAir()->create();
    $author = User::factory()->create();
    resolve(He4rt\Live\Chat\Actions\SendChatMessage::class)->execute($author, $live, 'mensagem imprópria');
    $message = Message::query()->sole();

    livewire(He4rt\PanelAdmin\Live\Resources\LiveResource\Widgets\LiveChatMessages::class, ['record' => $live])
        ->callAction(TestAction::make('deleteChatMessage')->table($message))
        ->assertNotified();

    expect(Message::query()->count())->toBe(0);
});
```

Run: `./vendor/bin/pest app-modules/panel-admin/tests/Feature/Live/LiveResourceTest.php`
Expected: FAIL.

- [ ] **Step 3: Implementar resource, pages, widgets**

Diretrizes concretas (o implementador materializa no padrão Filament 5 dos vizinhos):

- `LiveResource`: `model = Live::class`, navegação com `Heroicon::OutlinedVideoCamera`, label pt_BR `Lives`, grupo de navegação coerente com os vizinhos. Tabela: `title`, `status` (badge — o enum já tem cor/label), `started_at`/`ended_at` (`->dateTime('d/m/Y H:i')->timezone(config('app.display_timezone'))`), `peak_viewers`. Sem `EditAction` (mudanças passam pelas actions de domínio); `ViewAction` habilitada; `canCreate(): false` (criação é pela action custom).
- `ListLives`: header action `createLive` — Filament Action com schema (`TextInput::make('title')->required()->maxLength(255)`, `Textarea::make('description')`), `->action()` chamando `resolve(CreateLive::class)->execute(...)` dentro de try/catch de `CurrentLiveAlreadyExists` → `Notification::make()->danger()` com a mensagem da exceção (o teste "bloqueia criação" espera notificação e nenhum registro novo).
- `ViewLive`: infolist com title/description/status/started_at/ended_at/`peak_viewers` + entrada da URL RTMP (`rtmp://localhost:1935/live?user=he4rt&pass=` + key) e a `stream_key` com `->copyable()` e revelação (`TextEntry` com estado oculto/ação de revelar, padrão de secret dos vizinhos se houver). Header actions: `endLive` (`->requiresConfirmation()`, visível se status ≠ Ended, chama `EndLive`) e `rotateStreamKey` (`->requiresConfirmation()`, visível se status ≠ Ended, chama `RotateStreamKey`). Footer widgets: `LiveAudienceChart` e `LiveChatMessages`.
- `LiveAudienceChart` (ChartWidget de linha): dataset `viewers` de `LiveViewerSample::query()->where('live_id', $record->id)->orderBy('sampled_at')`, labels `sampled_at` no display timezone.
- `LiveChatMessages` (TableWidget): query `Message::query()->where('channel_id', $this->record->id)->latest('sent_at')` com colunas autor (`provider.user.username` ou metadata), `content`, `sent_at`; row action `deleteChatMessage` (`->requiresConfirmation()->color('danger')`) chamando `resolve(DeleteChatMessage::class)->execute($record, auth()->user())` + `Notification`.

- [ ] **Step 4: KB do admin**

`docs/admin/en/system/lives.md` com front matter (`title: Lives`, `icon: heroicon-o-video-camera`, `order` após os irmãos do grupo `system`) descrevendo: criar live, colar key no OBS, encerrar, rotacionar, ler o gráfico e moderar o chat.

- [ ] **Step 5: Testes, checks e commit**

Run: `./vendor/bin/pest app-modules/panel-admin/tests/Feature/Live/LiveResourceTest.php`
Expected: PASS.
Run: `./vendor/bin/pest app-modules/panel-admin/tests`
Expected: PASS.
Run: `vendor/bin/pint --dirty --format agent`
Run: `vendor/bin/phpstan analyse app-modules/panel-admin/src --ansi`

```bash
git add app-modules/panel-admin docs/admin composer.lock
git commit -m 'feat(panel-admin): gestão de lives com moderação de chat e gráfico de audiência'
```

---

### Task 10: Infra mediamtx — variante ffmpeg, hooks de sinal e CORS pinado

**Files:**

- Modify: `docker/mediamtx.Dockerfile`
- Modify: `docker/mediamtx/mediamtx.yml`
- Modify: `docker-compose.yml` (env do serviço `heartdevs-mediamtx`)
- Modify: `.env.example`

**Interfaces:**

- Consumes: `POST /live/ingest/webhook` + header `X-Live-Webhook-Secret` (Task 3).
- Produces: mediamtx chamando o webhook em online/offline; HLS com CORS restrito à origem do portal.

- [ ] **Step 1: Dockerfile**

```dockerfile
# syntax=docker/dockerfile:1.15.0

FROM bluenviron/mediamtx:1.20.1-ffmpeg

COPY docker/mediamtx/mediamtx.yml /mediamtx.yml
```

- [ ] **Step 2: mediamtx.yml — hooks + CORS**

Adicionar/alterar (mantendo o restante):

```yaml
hls: yes
hlsAddress: :8888
hlsVariant: lowLatency
hlsAllowOrigin: '$LIVE_PORTAL_ORIGIN'

paths:
    live:
        runOnOnline: |
            wget -q -O- --header="Content-Type: application/json" --header="X-Live-Webhook-Secret: $LIVE_WEBHOOK_SECRET" --post-data="{\"event\":\"online\",\"path\":\"$MTX_PATH\"}" http://host.docker.internal:8000/live/ingest/webhook
        runOnOffline: |
            wget -q -O- --header="Content-Type: application/json" --header="X-Live-Webhook-Secret: $LIVE_WEBHOOK_SECRET" --post-data="{\"event\":\"offline\",\"path\":\"$MTX_PATH\"}" http://host.docker.internal:8000/live/ingest/webhook
```

**Verificação obrigatória do implementador:** o mediamtx expande env vars no arquivo de config apenas via overrides `MTX_*`; dentro de `runOnOnline` o comando roda em shell do container, então `$LIVE_WEBHOOK_SECRET` e `$MTX_PATH` são resolvidos pelo shell — validar com `docker compose up` + `docker logs` que o comando dispara sem erro de expansão (o hook loga no start). Se `hlsAllowOrigin` não aceitar env var (config YAML não expande env), usar o override de env do mediamtx: `MTX_HLSALLOWORIGIN` no compose e REMOVER a chave do YAML.

- [ ] **Step 3: docker-compose + .env.example**

No serviço `heartdevs-mediamtx`:

```yaml
environment:
    LIVE_WEBHOOK_SECRET: '${LIVE_WEBHOOK_SECRET:-}'
    MTX_HLSALLOWORIGIN: '${APP_URL:-http://localhost:8000}'
```

`.env.example`: remover `LIVE_STREAM_KEY=` e adicionar `LIVE_WEBHOOK_SECRET=` no bloco de live (comentário do bloco aponta para a spec da fase 2).

- [ ] **Step 4: Validar e commitar**

Run: `docker compose config --quiet && docker compose build heartdevs-mediamtx && docker compose up -d heartdevs-mediamtx && sleep 2 && docker logs --tail 20 heartdevs-mediamtx`
Expected: container sobe; sem erro de config.

Run: `curl -s -H "Origin: http://localhost:8000" -o /dev/null -D- http://localhost:8888/live/index.m3u8 2>&1 | grep -i 'access-control-allow-origin'`
Expected: `Access-Control-Allow-Origin: http://localhost:8000` (e não `*`).

```bash
git add docker docker-compose.yml .env.example
git commit -m 'feat(live): webhook de sinal do mediamtx e cors do hls pinado ao portal'
```

---

### Task 11: Smoke E2E local

**Files:** nenhum novo (verificação; correções pontuais se o smoke revelar defeito).

**Interfaces:** consome tudo das Tasks 1–10.

- [ ] **Step 1: Preparar ambiente**

1. `.env` local: garantir `LIVE_WEBHOOK_SECRET=dev-webhook-secret` (NÃO commitar) e `BROADCAST_CONNECTION=reverb` + bloco REVERB preenchido pelo installer.
2. Migrar: `php artisan migrate`.
3. Laravel acessível pelo container: conferir `curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/live`; se não houver serve, iniciar `php artisan serve --host=0.0.0.0` em background (bind 0.0.0.0 é obrigatório p/ o mediamtx alcançar o host — registrar o PID e encerrar ao final apenas se iniciado aqui).
4. `docker compose up -d heartdevs-mediamtx heartdevs-redis`.
5. Criar uma live via tinker: `php artisan tinker --execute 'echo resolve(He4rt\Live\Actions\CreateLive::class)->execute("Smoke fase 2", null)->stream_key;'` — anotar a key.

- [ ] **Step 2: Validar pipeline**

Se `command -v ffmpeg` não existir, pular (a) e reportar smoke parcial.

a. Publicar com a key da live (15s) e conferir: HLS 200; `curl http://localhost:9997/v3/paths/get/live` com `"ready":true`; **status da live no banco virou `on_air`** (`php artisan tinker --execute 'echo He4rt\Live\Models\Live::query()->latest()->first()->status->value;'`) — prova de que o webhook funcionou.

```bash
timeout 15 ffmpeg -re -f lavfi -i testsrc=size=640x360:rate=30 -f lavfi -i sine=frequency=440 \
  -c:v libx264 -preset veryfast -tune zerolatency -c:a aac -f flv \
  'rtmp://localhost:1935/live?user=he4rt&pass=<key anotada>'
```

b. Key errada → recusada (mediamtx nega após 403 do hook).
c. Webhook com secret errado → `curl -X POST http://localhost:8000/live/ingest/webhook -H 'Content-Type: application/json' -H 'X-Live-Webhook-Secret: errado' -d '{"event":"online","path":"live"}'` → 403.
d. Página: `curl -s http://localhost:8000/live | grep -c 'data-live-channel'` → 1.
e. Chat via domínio: `php artisan tinker` criando um user de teste + `SendChatMessage` e conferindo a linha em `messages` com provider He4rtLives.
f. Heartbeat: duas requisições ao Livewire? (suficiente validar via tinker `RecordViewerHeartbeat` retornando 1 e `peak_viewers` atualizado).

- [ ] **Step 3: Encerrar e reportar**

Encerrar processos iniciados aqui; encerrar a live de smoke (`EndLive` via tinker). Reportar cada item com output real. Defeitos: corrigir na camada certa, testar o módulo afetado, commitar como `fix(...)`. Sem mudança de código → sem commit.

---

## Self-review (executado na escrita do plano)

- **Cobertura da spec:** provider (T1); model/ciclo (T2); auth hook/webhook/cache/rename (T3); ModerationType + DiscordSource (T4); chat domínio + eventos (T5); audiência (T6); Reverb/Echo (T7); portal (T8); admin + KB (T9); mediamtx/CORS (T10); smoke (T11). Agregados que passam a incluir chat: sem tarefa — é ausência deliberada de mudança, documentada na spec.
- **Consistência de tipos:** `StreamStatus` renomeado na T3 e consumido na T8; `ChatMessageData->toArray()` alimenta `ChatMessageSent->message` (T5) e o JS lê `event.message.authorUsername` (T8); eventos usam `broadcastAs` e o JS escuta com prefixo `.`; `LiveStatus` enum (T2) usado em T3/T8/T9.
- **Pontos de verificação obrigatória no lugar de suposição:** assinatura de `ResolveUserProviderDTO::make` (T5), forma de invocar `DiscordSource` nos testes (T4), expansão de env no mediamtx.yml (T10), padrão de auth dos testes do panel-admin (T9).
